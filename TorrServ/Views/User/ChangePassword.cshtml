@model TorrServ.ViewModels.ChangePasswordViewModel
@{
    ViewBag.Title = "Changing the password";
}

    <h2>Changing the password for the user @Model.Email</h2>
<form asp-action="Create" asp-controller="IdentityUser">
    <div asp-validation-summary="All" class="btn-dark"></div>
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="Email" />
    @*второй вариант*@
    @*<div class="form-group">
            <label asp-for="OldPassword" class="control-label">Старый пароль</label>
            <input type="text" asp-for="OldPassword" class="form-control" />
        </div>*@

    <div class="form-group btn-dark">
        <label asp-for="NewPassword" class="control-label">New password</label>
        <input type="text" asp-for="NewPassword" class="form-control" />
    </div>
    <div class="form-group btn-dark">
        <input type="submit" value="Save" class="btn btn-default" />
    </div>
</form>
